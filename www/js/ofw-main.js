// Initialize your app
var ofwF7App = new Framework7();

// Export selectors engine
var $$ = Dom7;
var mainView = ofwF7App.addView('.view-main', {
            // Because we use fixed-through navbar we can enable dynamic navbar
            dynamicNavbar: true
});


